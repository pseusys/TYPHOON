FROM python:3.12-alpine

RUN apk add --no-cache iproute2

COPY evaluation/background/common/ /common/
COPY evaluation/background/rtp_video/rtp_video.py /usr/local/bin/rtp_video
RUN chmod +x /usr/local/bin/rtp_video

ENV ROLE=server
CMD ["python3", "/usr/local/bin/rtp_video"]
